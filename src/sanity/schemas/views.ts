const viewsSchema = {
  name: 'views',
  title: 'Page Views',
  type: 'document',
  fields: [
    {
      name: 'count',
      title: 'Count',
      type: 'number',
    },
  ],
};

export default viewsSchema;
